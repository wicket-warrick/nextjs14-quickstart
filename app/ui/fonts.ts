import { Inter, Roboto, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const roboto = Roboto({ subsets: ['latin'], weight: ['100', '300'] });
export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: '400'
})
